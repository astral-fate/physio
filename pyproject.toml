[project]
name = "physio"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "openai>=1.0",
    "numpy>=1.24",
]

[tool.vercel]
entrypoint = "api.index:app"

[tool.vercel.scripts]
build = "python scripts/vercel_build.py"
